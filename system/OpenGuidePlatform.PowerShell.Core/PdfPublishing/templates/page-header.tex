% OpenGuidePlatform default running header (LaTeX preamble, Pandoc template).
% fancyhdr boxes are left-to-right, so right-to-left documents set the title
% with \RL and mirror the positions.
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
$if(dir)$
\fancyhead[R]{\small\RL{$if(short_title)$$short_title$$else$$title$$endif$}}
\fancyhead[L]{\small $labels.edition$ $edition$}
$else$
\fancyhead[L]{\small $if(short_title)$$short_title$$else$$title$$endif$}
\fancyhead[R]{\small $labels.edition$ $edition$}
$endif$
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{\fancyhf{}\renewcommand{\headrulewidth}{0pt}\fancyfoot[C]{\small\thepage}}
